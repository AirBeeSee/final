name: cha
on:
  workflow_dispatch:

jobs:


  docker-build:
  
    runs-on: ubuntu-latest
    steps:
        - name: checkout
          uses: actions/checkout@v4
        - run: |
            docker compose create
